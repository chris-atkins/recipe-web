
describe('the recipeService', function() {

	var recipeService;

	beforeEach(angular.mock.module('recipe.recipe.service'));

	it('does some shit with injecting', angular.mock.inject(function(recipeService) {
		expect(true).toBe(true);
	}));
});